doctype html
html
  head
    title= title
    link(rel='stylesheet' href='/stylesheets/style.css')
    meta(name="viewport" content="width=device-width, initial-scale=1")
  body
    - const filterErrorsByPath = (path) => errors && errors.filter(error => error.path === path);
    - const getErrorMsg = (errors) => errors[0]?.msg || "";
    header
      a(href="/")
        h1 Club House
      if user
        a(href="/logout" class="btn primary-btn" style="display:flex;align-items:center;justify-content:center;gap:0.4rem;min-width:100px") Logout <svg xmlns="http://www.w3.org/2000/svg" width="18" height="18" viewBox="0 0 24 24" fill="none" stroke="#fff" stroke-width="2" stroke-linecap="round" stroke-linejoin="round" class="feather feather-log-out"><path d="M9 21H5a2 2 0 0 1-2-2V5a2 2 0 0 1 2-2h4"></path><polyline points="16 17 21 12 16 7"></polyline><line x1="21" y1="12" x2="9" y2="12"></line></svg>
      else
        .btns-container
          a(href="/signup" class="btn secondary-btn") Signup
          a(href="/login" class="btn primary-btn") Login
    main
      block content