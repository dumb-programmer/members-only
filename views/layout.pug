doctype html
html
  head
    title= title
    link(rel='stylesheet' href='/stylesheets/style.css')
    meta(name="viewport" content="width=device-width, initial-scale=1")
  body
    header
      a(href="/")
        h1 Club House
      if(user)
        a(href="/logout" class="btn primary-btn") Logout
      else
        .btns-container
          a(href="/login" class="btn primary-btn") Login
          a(href="/signup" class="btn secondary-btn") Signup
    main
      block content